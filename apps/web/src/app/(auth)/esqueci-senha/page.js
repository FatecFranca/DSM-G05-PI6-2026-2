import { AuthForm } from '@/components/auth/auth-form';
export const metadata = { title: 'Recuperar senha | Estoque Inteligente' };
export default function ForgotPage() { return <AuthForm mode="forgot" />; }
