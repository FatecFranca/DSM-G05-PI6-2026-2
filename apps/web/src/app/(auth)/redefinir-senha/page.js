import { AuthForm } from '@/components/auth/auth-form';
export const metadata = { title: 'Redefinir senha | Estoque Inteligente' };
export default function ResetPage() { return <AuthForm mode="reset" />; }
